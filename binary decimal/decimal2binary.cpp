#include <stdio.h>


int len (char *origin){
	int len = 0;
	while(origin[len]!='\0'){
		len++;
	}
	return len;
}

char *decimal2binary(char *origin){
	int size = len(origin);
	char rest[32] ;
	 
}

int main(){
	char buf[100];
	scanf("%s",buf);

	return 0;
}